import { AppPhonePipe } from './app-phone.pipe';

describe('AppPhonePipe', () => {
  it('create an instance', () => {
    
    const pipe = new AppPhonePipe();

    expect(pipe).toBeTruthy();
  });
});
