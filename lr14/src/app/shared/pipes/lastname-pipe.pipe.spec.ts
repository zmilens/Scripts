import { LastnamePipePipe } from './lastname-pipe.pipe';

describe('LastnamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new LastnamePipePipe();
    expect(pipe).toBeTruthy();
  });
});
